fn main() {
    let result = 10.00;                                   //no type stated f64 by default
    let interest: f32 = 8.35;                              //single precision
    let cost: f64 = 1500.600;                               //double precision

    println!("result is {}", result);
    println!("interest is {}", interest);
    println!("cost is {}", cost);
}
